package Data;

class Comparators {

    public static void main(String [] args) {

        // Comparators:

            // allow the user to quickly compare elements within a set 
                // contains 2 functions:

            // public int compare(object1 x, object2 y) {
                //  returns a negative value if x < y
                // returns a positive value if x > y
                // returns 0 if x = y
            //  } 


            // public boolean equals(object x) {
                    // returns true, if the specified object is also a comparator
                    // and imposes the same ordering as this comparator
            //  }

            // used in implementation of various collections to ensure 
            // different variable types can be stored according to a single common ground
            // set of Geometric Objects, will be stored as different shapes
            // but their area, will all be stored as a double, so the comparator can organise them
            // in ascending order or whatever is required based on that information

    }

}
